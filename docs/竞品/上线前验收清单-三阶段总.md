# 上线前验收清单（阶段一业务 / 阶段二费用 / 阶段三建档）

> 代码侧状态：阶段一 624 ✅ → 阶段二 651 ✅ → 阶段三 679 ✅，ruff 全绿，全部已合入 feature/bill-import。
> 以下全部是**用户侧动作**，按「阻塞程度」排序。✅ 一项勾一项。

## A. 阻塞项（不做则功能不生效）

| # | 事项 | 影响面 | 怎么做 |
|---|---|---|---|
| ~~A1~~ | ~~补六个建档接口 URL 路径~~ ✅ **2026-08-14 已补 5/6** | 价格/客户/司机/车辆/工厂已到位（`s3.jxt56.com`，见逆推规范 §14）；委托人仍缺但本阶段只计数不建档，不阻塞 | 剩余动作：按下表填 `config/master_data.yaml` endpoints |
| A2 | **补 15 个 price_id + 确认「其它费」** | 阶段二长尾费目（现状：null 自动降级 skip_report） | 费用弹窗右键「费目」下拉框 → 检查 → Copy `<select>` outerHTML → 发我，我一次解析全量填 `config/fee_price_map.test.yaml` |
| A3 | **阈值语义确认** | 阶段三触发时机 | ≥5 次建档（现状）还是 >5 次（第 6 次）？后者只需 `threshold: 6` |
| A4 | **生产环境 price_id 整表** | 上线 | `fee_price_map.prod.yaml` 全 null，生产价格表到位后填；测试/生产 id 大概率不同 |

## B. 验证项（测试环境人工跑，代码已就绪）

| # | 事项 | 通过标准 |
|---|---|---|
| B1 | 带费测试单：应收运费+预提费、应付运费、车辆成本油费 | UI 四通道落位正确；金额/费目/合计正确；响应 `price[0].get/pay` + `supplier` 数组结构符合实证 |
| B2 | 对账抽样：秋怡一单（税金恒等）+ 一票长尾单（to_other 归并） | 恒等式成立；原名进备注；报告无解析失败 |
| B3 | **duo_get 双输入框**：填 2/3 保存抓 form-data 发我 | 确认语义后开「公司成本→duo_get」通道（配置改动，零代码） |
| B4 | **F2 验证**：提交一个新文本客户名，看 TMS 档案列表是否自动多一条 | 若自动建档 → 阈值策略要重估，告诉我 |
| B5 | 阶段三建档链路（A1 补齐后）：同客户连导 5 批 → 第 5 批建档 | 档案界面可见；订单 `c_id` 回填；报告「建档清单」正确 |
| B6 | 司机+车辆组合计数：同人不同车/同车不同人各计各的 | 报告计数正确（**注：司机建档已降级为只计数**——live 实测 bailor_id 主键唯一约束，见逆推规范 §11；车辆建档正常） |
| B7 | 降级验证：endpoints 故意留 TODO → 订单照常，报告 warning | 不阻塞 |

## C. 清理项（测试环境脏数据，防计数/对账干扰）

- [ ] 价格费目「测试」price_id=124823
- [ ] 客户「test」client_id=169642（**含随建的出车费 price_rate=10 价格记录**）
- [ ] 委托人「test」bailor_id=159132
- [ ] 司机「test」id=294233
- [ ] 车辆「沪C00000」truck_id=297329
- [ ] 工厂「test」factory_id=1881303
- [ ] **live smoke 新增（2026-08-14）**：TMS 搜索 `smoke` 全删——费目 1 条（price_id=124828）、客户 4 条（含 169672/169677）、工厂 3 条（1881705/1881706/1881709）、车辆 6 条（沪S88xxx，truck_id 297348/297349/297353 等）、司机组 1–2 条

## D. 遗留问题（已文档化，不阻塞）

| 问题 | 状态 |
|---|---|
| 税金 Σ=3193 口径解释（6386 行×4 元=25544 对不上） | ✅ **已答复（2026-08-14）**：实测 6386 行税金**全部为 0.5 元/行**（金额分布 0.5×6386），6386×0.5=3193.0 恒等成立；「4 元/行」是误记，25544 前提不成立。测试 `test_qiuyi_tax_identity` 断言行数=6386 且合计=3193.00（abs 0.01），每单恒等 ok=True |
| 651 vs 624+26 差 1 个用例 | ✅ **已答复（2026-08-14）**：以 pytest collect 为权威口径——阶段二新增用例**恰好 25 个**（test_fees.py 全新增，collect 634→659）；624/651 是不同时点 passed 数（golden 表格样本不入库、按存在性 skip，skip 数波动），passed 差 27 = 25 新用例 + 2 个 skip 状态差，与「新增用例数」是两码事；阶段三 +28（687−659）与 test_master_data.py 用例数精确一致 |
| driver_name 硬发是否 live 实证 | ✅ **已答复（2026-08-14）**：字段存在性=抓包实证（逆推规范 §2.7 cost 行已记录）；「缺键 204 拒单」仅程序注释记录（2026-08-13 live），**原始 dump 未留存**——硬发保持（多发键安全），§2.7 已补实证状态条，下次 live 验证补留 dump |
| TMS 档案查询/去重接口 | 无 → 本地 store 幂等 + sn_prefix 避让；后续抓到列表接口再加查重 |
| 请求侧 factory_id / 派车段 driver_id·car_id 回填键 | B5 验证时确认 |
| 多箱号 `b_num` 写法 | 未验证，现状首箱+warning |
| `type` 枚举全值（进口/倒箱/内装） | 仅 1=出口 实证 |
| 计数器单文件 JSON | 单进程 OK；多 worker/多实例部署需换共享存储 |

## E. 上线顺序建议

1. A1+A2+A3 补配置 → 2. B1–B7 测试环境全过 → 3. C 清脏数据 → 4. A4 生产价格表 + 生产 endpoints → 5. **确认部署为单 worker 单进程**（成功单注册表/建档计数均依赖进程内锁 + 单文件存储，多 worker/多实例需换共享存储）→ 6. 小批量真实账单试导（先应收单通道）→ 7. 全量。
