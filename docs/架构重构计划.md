# 架构重构计划

> 状态：已评审通过（2026-09-06），待执行
> 基线分支：`refactor/async-split-main`
> 基线数据：93 文件 / 22,255 行；全量测试 1,133 passed / 11 skipped；ruff 全绿
> 纪律：行为零变更（OpenAPI 契约 diff 为零）；每阶段独立提交、独立验证、完成即收手汇报

---

## 1. 现状结构（目录与职责）

```
app/
├── main.py (93)           组装入口 + 测试接缝宿主（⚠️ 职责越界，见问题 1）
├── core/ (1,081)          横切设施：config / errors / logging / executor(并发闸)
│                          skill_base / skill_registry / box_whitelist / 双日志存储
├── api/ (1,533)           表现层
│   ├── app_factory.py       应用组装
│   ├── middleware/          auth / rate_limit / access_log
│   ├── routes/              7 个薄路由
│   ├── bridges.py           路由→orders 域适配点
│   ├── response_shell.py    统一响应外壳
│   └── error_handlers / uploads
├── llm/ (315)             LLM 适配器（client）
├── mineru/ (494)          OCR 适配器（client / schema）
├── skills/tuoshu/ (7,108) 托书解析技能（11 文件，7 个超 600 行，占全项目 32%）
└── orders/ (11,625)
    ├── http_client.py       ⚠️ 域间共享 HTTP 设施放在域根（问题 5）
    ├── bill/ (7,653)        ✅ 已拆 5 子包：parsing / aggregation / fees / master_data / submission
    ├── manifest/ (1,587)    ⚠️ 扁平 5 文件未拆（parser 731 行）
    ├── document/ (1,581)    文档→订单（rules 787 / service 687）
    └── text/ (544)          文本→订单（mapper / extractor / client / schema）
```

架构范式：范式 2（按业务域分包）+ 单向依赖纪律（`docs/架构说明.md`、规范记忆已固化）。

### 实测包级依赖图（AST 分析）

```
routes ──→ service ──→ client ──→ llm / mineru          ✓ 单向（主体健康）
     ╲
      ╲──→ app.main ←── middleware / routes             ✗ 双向（问题 1，全项目唯一循环）
orders.document / orders.text ──→ skills.tuoshu          ✗ 跨域（问题 2）
```

---

## 2. 问题清单

| # | 级别 | 问题 | 证据位置 |
|---|---|---|---|
| 1 | P1 | **app.main 上帝模块 + 测试接缝倒挂**：main.py re-export 各层私有符号（`_summarize_import_response`、`_LIMITERS`、`_resolve_client_ip`、`_publish_order`、`build_result`），middleware / routes 再 lazy import app.main，形成全项目唯一双向依赖。根因：测试用 `setattr(main_module, ...)` 打桩，把组装层变成打桩宿主 | main.py:25-39、auth.py:27、rate_limit.py:169、bill_import.py:68、orders.py:45 |
| 2 | P1 | **orders → skills 跨域依赖**：document/service、document/rules、text/extractor 直接 import tuoshu 的 convert_service / normalizer，违反「禁止跨域互调」纪律 | document/service.py:22、rules.py:16、text/extractor.py:12 |
| 3 | P2 | **tuoshu 巨型文件群无分层**：converter 1,103（含 `#!/usr/bin/env python3` 残留）、post_checks 1,075、normalizer 871、convert_service 831、finalize 803、skill 735、post_common 623 | 行数统计 |
| 4 | P2 | **manifest 域与 bill 结构不一致**：bill 已 5 子包，manifest 扁平（parser 731 行）；`imported_registry.py` 双实现（normalize / reload / store_path 重复；锁模型不同：manifest 用 threading.Lock，bill 用 asyncio 锁） | 函数集合对比：共有 3 / 各自独有 4-5 |
| 5 | P2 | **http_client.py 位置暧昧**：放 orders/ 域根，被 bill / manifest / text 三子域消费——域间共享设施不应放某个域内 | 消费方 4 文件 |
| 6 | P3 | **命名问题**：① `legacy_template.py` vs `template_store.py` 双模板体系命名不达意；② routes 命名不一致（`orders.py` 资源名 vs `bill_import.py` 动作名）；③ bridges.py 以 `_` 私有名做跨层公开 API；④ `service.py` / `convert_service.py` / `skill.py` 三种服务命名并存 | 各文件头 |
| 7 | P3 | **bill/parsing/parser.py 1,000 行**：混装 _SheetView 抽象、magic 检测、L1/L2/L3 编排、exact 兜底、模板行读取五种职责 | 行数统计 |

---

## 3. 目标架构

```
api/routes → orders.{bill, manifest, document, text} → core / llm / mineru
skills/tuoshu → core / llm / mineru        （不再被 orders 反向引用）
main.py = 纯组装（~30 行，零 re-export）
app/core/ 收编：http_client + 跨域共享的文本归一工具
```

依赖硬约束（AST 复测断言）：

1. 全项目**零双向依赖**（`main ↔ api` 消除后应无任何环）；
2. `app/orders/**` 不出现 `from app.skills`；
3. `app/api/**` 不出现 `from app.main`（main 只被 uvicorn / 入口引用）。

---

## 4. tuoshu 域拆分设计

### 新目录结构

```
app/skills/tuoshu/
├── __init__.py                    技能注册入口（不变）
├── schema.py                      数据契约
├── prompt.py                      prompt 组装（410，独立保留）
├── convert/                       【拆自 converter.py 1,103 + convert_service.py 831】
│   ├── detect.py                  格式探测与分发
│   ├── office_docs.py             Word / WPS / OLE 文档解析
│   ├── spreadsheets.py            Excel 族解析
│   └── markdown_out.py            → markdown 输出（编排）
├── mapping/                       【拆自 deterministic_mapper 343 + finalize 803】
│   ├── fingerprint.py             模板指纹与命中
│   ├── field_mapping.py           确定性字段取值
│   └── order_mapping.py           结果→订单映射 + 人工复核标记
├── normalize/                     【拆自 normalizer.py 871】
│   ├── dates.py                   日期归一（跨域复用点，P3 先行处理）
│   ├── fields.py                  字段归一
│   └── containers.py              箱型/箱量归一
├── checks/                        【拆自 post_checks 1,075 + post_common 623】
│   ├── grounding.py               grounding 校验
│   ├── documents.py               单据/发货人校验
│   └── issues.py                  issue 管理（原 post_common）
└── skill.py                       技能编排入口（735 瘦身为纯编排）
```

### 拆分后依赖关系图

```
skill.py ──→ convert/（纯函数叶子，零内部依赖）
    ├──→ mapping/ ──→ normalize/
    ├──→ checks/  ──→ normalize/
    └──→ prompt/、schema/

规则：convert/ 不依赖 mapping / checks；normalize/ 零内部依赖（叶子）；
      checks/ 不依赖 convert/（只消费其输出结构）
```

---

## 5. 分阶段执行计划

执行顺序：**P1 → P2 → P3 → P4 → P5 → P6 → P7**（已评审确认，不变）。

### P0 基线固化（零代码）

- 记录测试 / ruff / 行数 / 依赖图快照（本文档 §1 即基线）；
- OpenAPI 契约快照落盘（后续每阶段 diff 对比）。

### P1 main 去上帝化（问题 1，方案 A）

- middleware / routes 内 5 处 `from app.main import ...` 改为模块直连引用；
- 测试打桩目标从 `setattr(main_module, ...)` 改为 `setattr(所在模块路径, ...)`（monkeypatch 原生支持模块路径打桩，无需 main 中转）；
- bridges.py 私有名转公开（`_parse_document_to_order` → `parse_document_to_order`）；
- main.py 回归纯组装（~30 行，删除全部 re-export 与别名导入）。

改动面：main + middleware×2 + routes×3 + 约 15 个测试文件的打桩路径。
验证：全量测试绿；AST 复测 `main ↔ api` 双向依赖消失。
风险：低（机械替换，测试即时暴露遗漏）。

### P2 http_client 上提（问题 5）

- `app/orders/http_client.py` → `app/core/http_client.py`；
- bill（master_data / submission）、manifest、text 共 4 处 import 更新。

验证：全量绿；AST 复测 orders 子域间无经由域根的共享引用。
风险：极低。

### P3 跨域解耦（问题 2）

**下沉位置决策（2026-09-06 评审拍板）：优先 `app/core/`，不新建 `document_parsers/` 层。**

- `normalizer.normalize_date_value` 等**被 document / text 复用的纯函数** → 下沉 `app/core/text_normalize.py`（tuoshu 改为从 core 引用，保持单一实现）；
- `convert_service`（文档转换编排，被 document/service 消费）→ 迁 `app/core/doc_convert.py`，tuoshu 编排改引 core；若迁移中发现其耦合 LLM / 技能状态则原地保留并另行评估（以实际代码为准，不强行搬）。

验证：全量绿；AST 复测 `app/orders/**` 无 `from app.skills`。
风险：中（函数移动需保持行为，golden 样本兜底）。

### P4 tuoshu 子包拆分（问题 3）

**拆分顺序（2026-09-06 评审调整）：先拆 `normalize/`（被跨域引用、是其他簇的依赖叶子），再拆 `convert/` → `checks/` → `mapping/`，分 4 个子提交。**

1. normalize/ 先行：P3 下沉后余下部分归位 `normalize/` 子包，mapping / checks 改引新路径；
2. convert/：converter.py 按格式族拆（detect / office_docs / spreadsheets / markdown_out），顺带删除 `#!/usr/bin/env python3` 残留；convert_service 并入；
3. checks/：post_checks + post_common 按校验域拆；
4. mapping/：deterministic_mapper + finalize 合并重组。

验证：每子提交全量绿；单文件 ≤500 行；tuoshu 内部依赖满足 §4 依赖规则。
风险：中（纯移动 + import 更新，行为零变更）。

### P5 manifest 对齐 bill 模式（问题 4 前半）

- 拆 `parsing/`（sheet 抽象 + 编排）与 `submission/`（client + registry）子包；
- parser 731 行拆分（对齐 bill/parsing 结构）。

验证：全量绿；结构与 bill 对称。
风险：中。

### P6 注册表公共工具抽取（问题 4 后半）

**范围明确（2026-09-06 评审拍板）：只抽公共工具，不合并锁模型。**

- 抽取 `normalize` / `store_path` / 原子落盘等纯函数 → `app/orders/registry_common.py`（或 core，视 P2 后归属再定）；
- **保留双实现**：bill 侧 asyncio 锁（`alock_for`，2026-09 异步化改造后生产语义）与 manifest 侧 threading.Lock 临界区语义不同，强行统一有并发缺陷风险；
- 若实施中发现两侧语义已实际分叉（键维度 / 登记时机不同），仅保留公共工具抽取，其余不动。

验证：全量绿；两侧注册表行为单测逐条对照。
风险：低（只动纯函数）。

### P7 命名统一 + 收尾（问题 6、7）

- `legacy_template.py` → `builtin_template.py`（或并入 template_store，以实施时耦合度定）；
- routes 命名对齐（资源名 / 动作名二选一全局一致）；
- `convert_service.py` 随 P4 消失；`service.py` / `skill.py` 命名口径写入 `docs/架构说明.md`；
- bill/parsing/parser.py 拆分：`_SheetView` → `parsing/sheet_view.py`、exact 兜底 → `parsing/exact_fallback.py`（parser 1,000 → ~600 行）；
- OpenAPI 契约终验 diff 为零；文档同步（架构说明 + 受影响接口文档）。

验证：全量绿 + ruff + OpenAPI diff 零。
风险：低。

---

## 6. 验证纪律（每阶段必做）

1. **全量回归**：`uv run pytest tests/ -q` 全绿（基线 1,133 passed / 11 skipped，数量只增不减）；
2. **静态检查**：`uv run ruff check app/ tests/` 零告警；
3. **AST 依赖图复测**（每阶段结束后执行一次，本文档附录脚本）：
   - 断言零双向依赖；
   - 断言 `app/orders/**` 无 `from app.skills`；
   - 断言 `app/api/**` 无 `from app.main`（P1 起生效）；
4. **OpenAPI 契约对比**：与 P0 快照 diff 为零（行为零变更）；
5. **分支纪律**：每阶段独立 feature 分支（`refactor/` 前缀，Conventional Commits 对齐），全绿后合入 `refactor/async-split-main`，合入后保留分支。

---

## 附录：AST 依赖图复测脚本

```python
"""dep_check.py — 包级依赖图 + 循环检测（架构重构各阶段复测用）。
用法：python dep_check.py [--assert-rules]
"""
import ast
import os
import sys
from collections import defaultdict

deps = defaultdict(set)

def mod_name(path: str) -> str:
    m = path[:-3].replace("/", ".").replace(".__init__", "")
    return m

for root, dirs, fs in os.walk("app"):
    dirs[:] = [d for d in dirs if d != "__pycache__"]
    for f in fs:
        if not f.endswith(".py"):
            continue
        path = os.path.join(root, f)
        m = mod_name(path)
        try:
            tree = ast.parse(open(path, encoding="utf-8").read())
        except SyntaxError:
            continue
        for node in ast.walk(tree):
            if isinstance(node, ast.Import):
                for a in node.names:
                    if a.name.startswith("app"):
                        deps[m].add(a.name)
            elif isinstance(node, ast.ImportFrom):
                if node.level > 0:
                    # 相对 import：module 为相对路径名（如 submission），
                    # 按 level 回溯包层级后拼出绝对模块名
                    base = m.split(".")
                    pkg = base if f == "__init__.py" else base[:-1]
                    for _ in range(node.level - 1):
                        pkg = pkg[:-1]
                    full = ".".join(pkg + ([node.module] if node.module else []))
                    deps[m].add(full)
                elif node.module and node.module.startswith("app"):
                    deps[m].add(node.module)

def pkg_of(m: str) -> str:
    parts = m.split(".")
    return ".".join(parts[:3]) if len(parts) >= 3 else m

pkg_deps = defaultdict(set)
for m, targets in deps.items():
    src = pkg_of(m)
    for t in targets:
        if pkg_of(t) != src:
            pkg_deps[src].add(pkg_of(t))

print("=== 包级依赖 ===")
for src in sorted(pkg_deps):
    print(f"{src} → {sorted(pkg_deps[src])}")

bidir = [
    (a, b)
    for a, ts in pkg_deps.items()
    for b in ts
    if b in pkg_deps and a in pkg_deps[b] and a < b
]
print(f"\n双向依赖: {bidir if bidir else '无'}")

# 函数级 lazy import 检查（app.api → app.main）
api_main = [
    m for m, ts in deps.items()
    if m.startswith("app.api") and any(t.startswith("app.main") for t in ts)
]
orders_skills = [
    m for m, ts in deps.items()
    if m.startswith("app.orders") and any(t.startswith("app.skills") for t in ts)
]
print(f"api → main 引用: {api_main or '无'}")
print(f"orders → skills 引用: {orders_skills or '无'}")

if "--assert-rules" in sys.argv:
    failures = []
    if bidir:
        failures.append(f"存在双向依赖: {bidir}")
    if api_main:
        failures.append(f"api 层仍引用 app.main: {api_main}")
    if orders_skills:
        failures.append(f"orders 域仍引用 skills: {orders_skills}")
    if failures:
        print("\n[FAIL] 架构规则违反：")
        for f_ in failures:
            print(f"  - {f_}")
        sys.exit(1)
    print("\n[PASS] 架构规则全部满足")
```

> 注：脚本暂存于本文档附录；P1 启动时落地为 `scripts/dep_check.py` 纳入版本管理，各阶段结束时执行 `python scripts/dep_check.py --assert-rules`。

---

## 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-06 | 初版（评审通过）；按评审意见调整：P3 下沉优先 core/、P4 先拆 normalize/、P6 只抽公共工具不合并锁模型、每阶段 AST 复测 |
